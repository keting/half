# AGENTS.md

本文件为 Codex 在本仓库内工作的项目规则，作用域为当前目录及其所有子目录。

## 测试规则

- 需要运行测试时，不要在本地宿主环境中直接安装依赖或直接执行测试命令。
- 测试必须在新建的测试容器中执行，避免污染本地环境。
- 如需新增或调整测试运行方式，优先使用项目现有的 Docker / docker compose 约定。
- 测试完成后，应清理本次创建的临时测试容器，除非用户明确要求保留。
